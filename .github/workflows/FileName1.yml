name: FileName1
run-name: Build [${{inputs.packages}}]

on:
  workflow_dispatch:
    inputs:
      packages:
        description: "Packages"
        required: true
        
jobs:

  build:
    name: ${{ inputs.packages }}
    runs-on: ubuntu-24.04-arm
    strategy:
      matrix:
        arch: [aarch64, arm]
      fail-fast: false
      
    steps:
  
    - name: Source
      run: echo 123
        
